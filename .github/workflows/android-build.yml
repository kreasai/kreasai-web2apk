name: Android Build (Web2APK)

on:
  workflow_dispatch:
    inputs:
      job_id:
        required: true
        type: string
      source_type:
        required: true
        type: string
      source_url:
        required: false
        type: string
      source_zip_path:
        required: false
        type: string
      icon_path:
        required: false
        type: string
      app_name:
        required: true
        type: string
      package_name:
        required: true
        type: string
      version_name:
        required: true
        type: string
      version_code:
        required: true
        type: string
      permissions:
        required: false
        type: string
      features:
        required: false
        type: string

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JOB_ID: ${{ github.event.inputs.job_id }}
      SOURCE_TYPE: ${{ github.event.inputs.source_type }}
      SOURCE_URL: ${{ github.event.inputs.source_url }}
      APP_NAME: ${{ github.event.inputs.app_name }}
      PACKAGE_NAME: ${{ github.event.inputs.package_name }}
      VERSION_NAME: ${{ github.event.inputs.version_name }}
      VERSION_CODE: ${{ github.event.inputs.version_code }}
      ICON_PATH: ${{ github.event.inputs.icon_path }}
      PERMISSIONS_JSON: ${{ github.event.inputs.permissions }}
      FEATURES_JSON: ${{ github.event.inputs.features }}
      WEB2APK_CALLBACK_URL: ${{ secrets.WEB2APK_CALLBACK_URL }}
      WEB2APK_CALLBACK_TOKEN: ${{ secrets.WEB2APK_CALLBACK_TOKEN }}
    steps:
      - name: Notify running
        shell: bash
        run: |
          if [ -n "$WEB2APK_CALLBACK_URL" ] && [ -n "$WEB2APK_CALLBACK_TOKEN" ]; then
            curl -sS -X POST "$WEB2APK_CALLBACK_URL" \
              -H "Content-Type: application/json" \
              -H "X-WEB2APK-CALLBACK-TOKEN: $WEB2APK_CALLBACK_TOKEN" \
              -d "{\"job_id\":$JOB_ID,\"status\":\"running\",\"progress\":20,\"message\":\"Initializing Android build runner\"}" >/dev/null || true
          fi

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Prepare web source and build flags
        shell: bash
        run: |
          mkdir -p android-template/app/src/main/assets
          rm -rf android-template/app/src/main/assets/*

          WEB_URL="$SOURCE_URL"
          if [ "$SOURCE_TYPE" = "zip" ]; then
            SOURCE_ENDPOINT="${WEB2APK_CALLBACK_URL%/callback}/source/${JOB_ID}?token=${WEB2APK_CALLBACK_TOKEN}"
            curl -fL "$SOURCE_ENDPOINT" -o /tmp/source.zip
            unzip -o /tmp/source.zip -d android-template/app/src/main/assets
            WEB_URL="file:///android_asset/index.html"
          fi

          export WEB_URL
          python3 - <<'PY' >> "$GITHUB_ENV"
          import json
          import os

          features = {}
          permissions = []
          try:
              features = json.loads(os.environ.get("FEATURES_JSON") or "{}")
          except Exception:
              features = {}
          try:
              permissions = json.loads(os.environ.get("PERMISSIONS_JSON") or "[]")
          except Exception:
              permissions = []

          def as_bool(value, default):
              if isinstance(value, bool):
                  return "true" if value else "false"
              return "true" if default else "false"

          print(f"WEB2APK_WEB_URL={os.environ.get('WEB_URL', '')}")
          print(f"WEB2APK_PERMISSIONS_CSV={','.join([str(x) for x in permissions])}")
          print(f"WEB2APK_ENABLE_SWIPE_REFRESH={as_bool(features.get('enable_swipe_refresh'), True)}")
          print(f"WEB2APK_ENABLE_EXTERNAL_APPS={as_bool(features.get('enable_external_apps'), True)}")
          print(f"WEB2APK_ENABLE_OFFLINE_PAGE={as_bool(features.get('enable_offline_page'), True)}")
          print(f"WEB2APK_ENABLE_BACK_NAVIGATION={as_bool(features.get('enable_back_navigation'), True)}")
          print(f"WEB2APK_ENABLE_SPLASH={as_bool(features.get('enable_splash'), False)}")
          print(f"WEB2APK_SPLASH_BG={str(features.get('splash_background_color') or '#111827')}")
          
          # Logic: If splash logo URL is empty but we have an icon path, fallback to icon path
          splash_logo = str(features.get('splash_logo_url') or '')
          if not splash_logo and os.environ.get('ICON_PATH'):
              splash_logo = f"{os.environ.get('WEB2APK_CALLBACK_URL').replace('/callback', '')}/icon/{os.environ.get('JOB_ID')}?token={os.environ.get('WEB2APK_CALLBACK_TOKEN')}"
              
          print(f"WEB2APK_SPLASH_LOGO={splash_logo}")
          PY


      - name: Download and prepare icon
        shell: bash
        run: |
          if [ -n "$ICON_PATH" ]; then
            # Ensure ImageMagick is installed for resizing
            sudo apt-get update && sudo apt-get install -y imagemagick
            
            ICON_ENDPOINT="${WEB2APK_CALLBACK_URL%/callback}/icon/${JOB_ID}?token=${WEB2APK_CALLBACK_TOKEN}"
            curl -fL "$ICON_ENDPOINT" -o /tmp/raw_icon.png
            
            # Auto-convert to PNG and resize to a strict square (forcing 512x512 master)
            # This ensures whatever format the user uploads, Android accepts it.
            convert /tmp/raw_icon.png -background none -gravity center -resize 512x512^ -extent 512x512 -format png /tmp/master_icon.png
            
            # Create rounded version just in case standard launcher demands it
            convert /tmp/master_icon.png \( +clone -alpha extract -draw "fill black polygon 0,0 0,512 512,512 512,0" -fill white -draw "circle 256,256 256,0" \) -alpha off -compose CopyOpacity -composite /tmp/master_icon_round.png
            
            # Create standard res folders
            mkdir -p android-template/app/src/main/res/mipmap-mdpi
            mkdir -p android-template/app/src/main/res/mipmap-hdpi
            mkdir -p android-template/app/src/main/res/mipmap-xhdpi
            mkdir -p android-template/app/src/main/res/mipmap-xxhdpi
            mkdir -p android-template/app/src/main/res/mipmap-xxxhdpi
            
            # Generate sizes
            convert /tmp/master_icon.png -resize 48x48 /tmp/ic_mdpi.png
            convert /tmp/master_icon.png -resize 72x72 /tmp/ic_hdpi.png
            convert /tmp/master_icon.png -resize 96x96 /tmp/ic_xhdpi.png
            convert /tmp/master_icon.png -resize 144x144 /tmp/ic_xxhdpi.png
            convert /tmp/master_icon.png -resize 192x192 /tmp/ic_xxxhdpi.png
            
            convert /tmp/master_icon_round.png -resize 48x48 /tmp/ic_mdpi_round.png
            convert /tmp/master_icon_round.png -resize 72x72 /tmp/ic_hdpi_round.png
            convert /tmp/master_icon_round.png -resize 96x96 /tmp/ic_xhdpi_round.png
            convert /tmp/master_icon_round.png -resize 144x144 /tmp/ic_xxhdpi_round.png
            convert /tmp/master_icon_round.png -resize 192x192 /tmp/ic_xxxhdpi_round.png
            
            # Overwrite the default icon
            cp /tmp/ic_mdpi.png android-template/app/src/main/res/mipmap-mdpi/ic_launcher.png
            cp /tmp/ic_hdpi.png android-template/app/src/main/res/mipmap-hdpi/ic_launcher.png
            cp /tmp/ic_xhdpi.png android-template/app/src/main/res/mipmap-xhdpi/ic_launcher.png
            cp /tmp/ic_xxhdpi.png android-template/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
            cp /tmp/ic_xxxhdpi.png android-template/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
            
            cp /tmp/ic_mdpi_round.png android-template/app/src/main/res/mipmap-mdpi/ic_launcher_round.png
            cp /tmp/ic_hdpi_round.png android-template/app/src/main/res/mipmap-hdpi/ic_launcher_round.png
            cp /tmp/ic_xhdpi_round.png android-template/app/src/main/res/mipmap-xhdpi/ic_launcher_round.png
            cp /tmp/ic_xxhdpi_round.png android-template/app/src/main/res/mipmap-xxhdpi/ic_launcher_round.png
            cp /tmp/ic_xxxhdpi_round.png android-template/app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.png
          fi

      - name: Build debug APK + unsigned release AAB
        shell: bash
        run: |
          cd android-template
          gradle --no-daemon assembleDebug bundleRelease \
            -Pweb2apkAppName="$APP_NAME" \
            -Pweb2apkApplicationId="$PACKAGE_NAME" \
            -Pweb2apkVersionName="$VERSION_NAME" \
            -Pweb2apkVersionCode="$VERSION_CODE" \
            -Pweb2apkWebUrl="$WEB2APK_WEB_URL" \
            -Pweb2apkPermissionsCsv="$WEB2APK_PERMISSIONS_CSV" \
            -Pweb2apkEnableSwipeRefresh="$WEB2APK_ENABLE_SWIPE_REFRESH" \
            -Pweb2apkEnableExternalApps="$WEB2APK_ENABLE_EXTERNAL_APPS" \
            -Pweb2apkEnableOfflinePage="$WEB2APK_ENABLE_OFFLINE_PAGE" \
            -Pweb2apkEnableBackNavigation="$WEB2APK_ENABLE_BACK_NAVIGATION" \
            -Pweb2apkEnableSplash="$WEB2APK_ENABLE_SPLASH" \
            -Pweb2apkSplashBackgroundColor="$WEB2APK_SPLASH_BG" \
            -Pweb2apkSplashLogoUrl="$WEB2APK_SPLASH_LOGO"

      - name: Upload build artifact
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: web2apk-apk-${{ github.event.inputs.job_id }}
          path: android-template/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload unsigned AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: web2apk-aab-${{ github.event.inputs.job_id }}
          path: android-template/app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error

      - name: Notify success
        if: success()
        shell: bash
        run: |
          if [ -n "$WEB2APK_CALLBACK_URL" ] && [ -n "$WEB2APK_CALLBACK_TOKEN" ]; then
            curl -sS -X POST "$WEB2APK_CALLBACK_URL" \
              -H "Content-Type: application/json" \
              -H "X-WEB2APK-CALLBACK-TOKEN: $WEB2APK_CALLBACK_TOKEN" \
              -d "{\"job_id\":$JOB_ID,\"status\":\"success\",\"stage\":\"builder\",\"progress\":100,\"message\":\"Builder workflow completed\",\"github_run_id\":${GITHUB_RUN_ID},\"artifact_name\":\"web2apk-apk-${JOB_ID}\",\"artifact_name_aab\":\"web2apk-aab-${JOB_ID}\"}" >/dev/null || true
          fi

      - name: Notify failure
        if: failure()
        shell: bash
        run: |
          if [ -n "$WEB2APK_CALLBACK_URL" ] && [ -n "$WEB2APK_CALLBACK_TOKEN" ]; then
            curl -sS -X POST "$WEB2APK_CALLBACK_URL" \
              -H "Content-Type: application/json" \
              -H "X-WEB2APK-CALLBACK-TOKEN: $WEB2APK_CALLBACK_TOKEN" \
              -d "{\"job_id\":$JOB_ID,\"status\":\"failed\",\"stage\":\"builder\",\"progress\":100,\"error_message\":\"Builder workflow failed\"}" >/dev/null || true
          fi
